CGROUP	GROUP	TEXT
TEXT	CSEG

liogpoin_::
	PUSH	DS
	PUSH	ES
	PUSH	SS
	PUSH	BX
	PUSH	CX
	PUSH	DX
	PUSH	BP
	PUSH	SP
	PUSH	SI
	PUSH	DI

	SHR	AX,4
	ADD	AX,BX
	INC	AX
	MOV	DS,AX
	MOV	ES,AX

	XOR	BX,BX
	MOV	DS:[0].W,CX	;X
	MOV	DS:[2].W,DX	;Y

	INT	0afh

	POP	DI
	POP	SI
	POP	SP
	POP	BP
	POP	DX
	POP	CX
	POP	BX
	POP	SS
	POP	ES
	POP	DS

	RET
